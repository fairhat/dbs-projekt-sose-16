'use strict'
const Boom = require('Boom')

/**
 * @name ZwingerController
 * @class
 * @desc Controller für Zwinger Views
 */
class ZwingerController {

  /**
   *
   * @name index
   * @desc index of zwinger
   * @version 0.0.1
   * @param {Request} req
   * @param {Response} res
   */
  static index(req, res) {
    return res()
  }

  static getTop(req, res) {
    res()
  }
}

module.exports = ZwingerController
